# Merge symbolic expressions during oapply_machine composition

Merge symbolic expressions during oapply_machine composition

## Usage

``` r
merge_machine_expressions(d, machines, all_snames, offsets, state_counts)
```
