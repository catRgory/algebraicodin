# Merge readout expressions during oapply_machine composition

Merge readout expressions during oapply_machine composition

## Usage

``` r
merge_machine_readout_expr(d, machines, all_snames, offsets)
```
