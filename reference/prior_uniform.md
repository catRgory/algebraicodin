# Uniform prior

Uniform prior

## Usage

``` r
prior_uniform(min = 0, max = 1)
```

## Arguments

- min:

  Lower bound (default 0)

- max:

  Upper bound (default 1)

## Value

A prior specification for
[`build_prior()`](https://catrgory.github.io/algebraicodin/reference/build_prior.md)
