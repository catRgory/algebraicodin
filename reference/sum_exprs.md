# Sum a list of expressions into a single expression

Sum a list of expressions into a single expression

## Usage

``` r
sum_exprs(exprs)
```

## Arguments

- exprs:

  List of language objects to sum

## Value

A single expression representing the sum
