# Rename variables in an expression

Rename variables in an expression

## Usage

``` r
rename_expr_vars(expr, mapping)
```

## Arguments

- expr:

  A language object (quoted R expression)

- mapping:

  Named character vector: old_name -\> new_name

## Value

The expression with names substituted
