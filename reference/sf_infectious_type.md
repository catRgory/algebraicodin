# SF type template for simple infectious disease models

One stock type (pop), with flow types for disease transitions and strata
transitions. Analogous to
[`infectious_ontology()`](https://catrgory.github.io/algebraicodin/reference/infectious_ontology.md).

## Usage

``` r
sf_infectious_type()
```

## Value

A StockFlowModel usable as a type template
