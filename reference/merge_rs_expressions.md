# Merge symbolic expressions during oapply_dynam composition

Merge symbolic expressions during oapply_dynam composition

## Usage

``` r
merge_rs_expressions(
  sharers,
  all_snames,
  composite_names,
  composite_map,
  inverse_map,
  n_composite,
  nboxes,
  box_states
)
```
