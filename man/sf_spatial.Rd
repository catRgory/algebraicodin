% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stockflow.R
\name{sf_spatial}
\alias{sf_spatial}
\title{Create a spatial (multi-patch) model via stratification}
\usage{
sf_spatial(
  base,
  patch_names,
  flow_types = NULL,
  migration = list(),
  contact = NULL
)
}
\arguments{
\item{base}{A StockFlowModel (the within-patch disease model)}

\item{patch_names}{Character vector of patch names}

\item{flow_types}{Named character: base flow name -> type (default all "disease")}

\item{migration}{Named list of migration specs. Names are migration flow names.
Each entry is a list with:
\itemize{
\item \code{from}, \code{to}: patch names
\item \code{rate}: quoted R expression (use the source patch name as the
stock variable, e.g., \code{quote(m * patch1)})
\item \code{params}: character vector of parameter names used in rate
}}

\item{contact}{Optional string: contact matrix parameter prefix. When provided,
transmission between patches uses a contact matrix (frequency-dependent).
Contact parameters are named \code{<prefix>_<to>_<from>}.}
}
\value{
A stratified StockFlowModel with spatial structure
}
\description{
Convenience wrapper around \code{\link{sf_stratify}} for spatial models.
Each patch runs independent disease dynamics, with optional migration
between patches and optional cross-patch transmission via a contact matrix.
}
