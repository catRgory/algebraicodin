% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrices.R
\name{vectorfield}
\alias{vectorfield}
\title{Generate a pure-R mass-action vector field function}
\usage{
vectorfield(pn, rate_names = NULL)
}
\arguments{
\item{pn}{A Petri net ACSet}

\item{rate_names}{Optional named vector mapping transition names to rate parameter names}
}
\value{
A function(t, state, parms) suitable for deSolve::ode
}
\description{
Generate a pure-R mass-action vector field function
}
\examples{
sir <- labelled_petri_net(
  c("S", "I", "R"),
  inf = c("S", "I") \%=>\% c("I", "I"),
  rec = "I" \%=>\% "R"
)
vf <- vectorfield(sir)
state <- c(S = 990, I = 10, R = 0)
parms <- list(inf = 0.001, rec = 0.1)
# Evaluate derivatives at t = 0
dstate <- vf(0, state, parms)
dstate[[1]] # named numeric vector of derivatives
}
