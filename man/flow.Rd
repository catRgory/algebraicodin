% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stockflow.R
\name{flow}
\alias{flow}
\title{Create a flow specification}
\usage{
flow(from = NULL, to = NULL, rate)
}
\arguments{
\item{from}{Source stock name (NULL or "cloud" for external inflow)}

\item{to}{Target stock name (NULL or "cloud" for external outflow)}

\item{rate}{Rate expression (quoted), \verb{function(u, p, t)}, or
\verb{function(u, sv, p, t)} for advanced use with sum variables}
}
\value{
A flow spec list
}
\description{
Create a flow specification
}
\examples{
# Internal flow between stocks
f1 <- flow(from = "S", to = "I", rate = quote(beta * S * I / N))

# External inflow (birth)
f2 <- flow(from = "cloud", to = "S", rate = quote(mu * N))

# External outflow (death)
f3 <- flow(from = "I", to = "cloud", rate = quote(mu * I))
}
