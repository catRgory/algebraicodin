% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_odin.R
\name{pn_to_odin_system}
\alias{pn_to_odin_system}
\alias{to_odin_system}
\title{Generate odin2 code and return it as a callable odin_system generator}
\usage{
pn_to_odin_system(
  pn,
  type = c("ode", "dde", "stochastic", "discrete"),
  rate_style = "mass_action",
  delays = NULL,
  compare = NULL
)

to_odin_system(
  pn,
  type = c("ode", "dde", "stochastic", "discrete"),
  rate_style = "mass_action",
  delays = NULL,
  compare = NULL
)
}
\arguments{
\item{pn}{A Petri net ACSet (LabelledPetriNet or ReactionNet)}

\item{type}{Model type:
\itemize{
\item \code{"ode"}: continuous ODE with \code{deriv()}
\item \code{"dde"}: delay differential equation with \code{deriv()} + \code{delay()}
\item \code{"stochastic"}: discrete-time stochastic with \code{update()} + \code{Binomial()}
\item \code{"discrete"}: discrete-time deterministic Euler with \code{update()}
}}

\item{rate_style}{Currently only \code{"mass_action"} is implemented.
\code{"custom"} is reserved and will error if requested.}

\item{delays}{Named list for DDE: maps transition names to delay specs.
Each element is a list with \code{tau} (numeric delay time) and optionally
\code{species} (character vector of which input species are delayed).}

\item{compare}{Optional named list mapping observed variables to
distribution families (e.g., \code{list(cases = "Poisson")})}
}
\value{
A function that creates an odin system, or a code string
}
\description{
If odin2 is available, this returns a function that when called creates
an odin system. Otherwise returns the code string.
}
\examples{
\donttest{
sir <- labelled_petri_net(
  c("S", "I", "R"),
  inf = c("S", "I") \%=>\% c("I", "I"),
  rec = "I" \%=>\% "R"
)
gen <- pn_to_odin_system(sir, type = "ode")
sys <- gen() # creates an odin2 system (requires odin2)
}
}
