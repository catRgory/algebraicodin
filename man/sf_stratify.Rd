% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stockflow.R
\name{sf_stratify}
\alias{sf_stratify}
\title{User-friendly stock-flow stratification}
\usage{
sf_stratify(
  base,
  strata_names,
  flow_types = NULL,
  cross_strata_flows = list(),
  param_types = NULL,
  mixing = NULL
)
}
\arguments{
\item{base}{A StockFlowModel (the base disease model)}

\item{strata_names}{Character vector of strata names (e.g., c("child", "adult", "senior"))}

\item{flow_types}{Named character: flow name -> "disease" or "strata"}

\item{cross_strata_flows}{Optional list of cross-strata flow specs. Each is a list
with \code{from}, \code{to} (strata names), \code{rate} (expression).}

\item{param_types}{Optional named character: param name -> type param name}

\item{mixing}{Optional named list specifying contact-matrix mixing for flows.
Names are base flow names (e.g., "infection"). Values are either:
\itemize{
\item A string: the contact matrix parameter prefix (e.g., "C").
Contact parameters will be named \code{C_<from>_<to>}.
\item A list with elements: \code{contact} (prefix), \code{type}
("frequency" or "density", default "frequency").
}
When mixing is applied, the flow rate uses a force-of-infection
that sums infectious pressure from all strata weighted by contact rates.}
}
\value{
A stratified StockFlowModel
}
\description{
Stratifies a stock-flow model by strata (e.g., age groups, locations).
}
