% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_comparison}
\alias{plot_comparison}
\title{Plot a comparison of two ODE solutions (e.g., R vs Julia)}
\usage{
plot_comparison(
  sol1,
  sol2,
  vars = NULL,
  labels = c("Model 1", "Model 2"),
  title = NULL
)
}
\arguments{
\item{sol1}{First solution (data.frame with time column)}

\item{sol2}{Second solution (data.frame with time column)}

\item{vars}{Character vector of variable names to compare}

\item{labels}{Two-element character vector labeling each solution}

\item{title}{Optional plot title}
}
\value{
A ggplot2 object
}
\description{
Plot a comparison of two ODE solutions (e.g., R vs Julia)
}
\examples{
t <- seq(0, 10, 0.1)
sol1 <- data.frame(time = t, S = 990 * exp(-0.3 * t), I = 10 * exp(0.1 * t))
sol2 <- data.frame(time = t, S = 980 * exp(-0.3 * t), I = 20 * exp(0.1 * t))
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_comparison(sol1, sol2, labels = c("Run 1", "Run 2"))
}
}
